# npmjs registry for this scoped public package.
@eamode:registry=https://registry.npmjs.org/

# For local semantic-release publishing, authenticate to npmjs first:
# npm login --scope=@eamode --registry=https://registry.npmjs.org/
#
# Alternatively set an auth token in your shell for a single release:
# NPM_TOKEN=YOUR_NPM_TOKEN npm run release:local
#
# Do not commit real auth tokens to this file.
